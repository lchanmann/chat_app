<h1>Log in as:</h1>
<div class="user-list">
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, sessions_path(user_id: user.id), method: :post %>
      </li>
    <% end %>
  </ul>
</div>
